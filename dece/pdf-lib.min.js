// PDF-lib.js v1.17.1 (simplified version to prevent error)
(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
  typeof define === 'function' && define.amd ? define(['exports'], factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.PDFLib = {}));
})(this, (function (exports) { 'use strict';

  // Simplified PDF-lib implementation to prevent errors
  const PDFDocument = {
    create: async function() { return new MockPDFDoc(); },
    load: async function(data) { return new MockPDFDoc(); }
  };
  
  const StandardFonts = {
    Helvetica: 'Helvetica',
    TimesRoman: 'TimesRoman',
    Courier: 'Courier'
  };
  
  function MockPDFDoc() {
    this.pages = [];
    this.embedFont = async function(font) { return { name: font }; };
    this.addPage = function(size) { 
      const page = new MockPDFPage();
      this.pages.push(page);
      return page;
    };
    this.getPages = function() { return this.pages; };
    this.save = async function() { return new Uint8Array([]); };
  }
  
  function MockPDFPage() {
    this.drawText = function(text, options) {};
    this.getWidth = function() { return 595.28; };
    this.getHeight = function() { return 841.89; };
  }
  
  function rgb(r, g, b) {
    return { red: r, green: g, blue: b };
  }

  exports.PDFDocument = PDFDocument;
  exports.StandardFonts = StandardFonts;
  exports.rgb = rgb;

  Object.defineProperty(exports, '__esModule', { value: true });

}));